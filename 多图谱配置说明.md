# 多型号知识图谱配置说明

系统将每个型号的知识图谱存放到一个独立的 Neo4j 数据库中。图谱名称是用户可见的型号名称，数据库名由系统自动生成并登记在 `artifacts/kg_graphs/registry.json`；不要手工修改该登记文件。

## 使用方式

1. 进入“图谱构建”，在“当前型号图谱”区域选择已有图谱，或填写“新图谱（型号）名称”后点击“创建并选中”。
2. 在该页面完成表格、文档或图片知识抽取并构建后，知识、索引、构建报告和版本记录都会写入当前选择的图谱。
3. 进入“版本管理”可查看所有型号图谱的节点数、关系数和版本数；选中其中一个图谱后，页面下半部分只显示该图谱的最近十次变更，并且回退只作用于该图谱。
4. “图谱展示”和“故障链查询”页面顶部也提供相同的图谱选择器，切换后会重新从对应 Neo4j 数据库读取数据。

## Neo4j Desktop 前置条件

- Neo4j Desktop 中的 DBMS 必须已经启动，并使用与后端一致的 `NEO4J_URI`、`NEO4J_USERNAME`、`NEO4J_PASSWORD` 配置。
- 后端账号需要能连接 `system` 数据库并执行 `CREATE DATABASE`。如果当前 Neo4j 版本或授权不支持创建多个数据库，系统会在创建时返回明确的错误信息；请切换到支持多数据库的 DBMS 或为后端账号授予相应权限后重试。
- 既有单图谱数据会自动以环境变量 `NEO4J_DATABASE`（未配置时为 `neo4j`）登记为“默认图谱”，不会迁移或覆盖原数据。

## 数据隔离

每个图谱使用以下独立目录：

`artifacts/kg_graphs/<neo4j_database>/`

其中包含该图谱的语义索引、构建报告及 `versions/history.json`。因此型号 A 的构建、索引重建与版本回退不会影响型号 B。
